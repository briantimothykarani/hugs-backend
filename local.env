# This file is read by Django's settings.py and docker-compose.yml.

# REMEMBER TO REPLACE ALL PLACEHOLDER VALUES WITH YOUR SECRETS.

# --- Application Secrets and Keys ---

SECRET_KEY=your-strong-django-secret-key-here-12345!
DEBUG=True

# --- Database Configuration (PostgreSQL) ---

# These variables configure the 'db' service in docker-compose.yml

POSTGRES_USER=hugs_user
POSTGRES_PASSWORD=hugs_password
POSTGRES_DB=hugs_db

# This DATABASE_URL connects the 'web' service (Django) to the 'db' service (Postgres)

# using their container names and ports.

DATABASE_URL=postgres://hugs_user:hugs_password@db:5432/hugs_db

# --- Channel Layer (Redis) ---

# This variable connects the 'web' service (Django Channels) to the 'redis' service.

REDIS_URL=redis://redis:6379/0

# --- OAuth and Email Settings ---

# These variables are accessed by Django settings using env()

OAUTH_GOOGLE_CLIENT_ID=your_google_client_id
OAUTH_GOOGLE_SECRET=your_google_secret
EMAIL_HOST_USER=karanibriantimothy@gmail.com
EMAIL_HOST_PASSWORD=your_gmail_app_password
